# 依赖与构建产物（减少索引体积与 token 消耗）
node_modules/
dist/
.yarn/cache/

# 敏感运行时数据（账号、令牌、本地数据库）
.env
.env.*
!.env.example
mc-tokens/
data/
*.db
*.db-journal

# 锁文件（体积大，改代码时通常不需要）
yarn.lock
package-lock.json

# Git 与编辑器
.git/
.vscode/
*.log
*.zip

# 归档目录（若存在旧版 mchatbot 副本）
mchatbot/
